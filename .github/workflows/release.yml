name: Release

on:
  push:
    branches: [main]

# last-pending-wins — semantic-release is idempotent over accumulated commits
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    permissions:
      contents: write
    uses: ./.github/workflows/semver-release.yml
